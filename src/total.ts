export const totalTodo = (subToTal: number, cuatoEsDeIva: number) =>{
    return subToTal + cuatoEsDeIva;
}
/**
 * PARAMETROS QUE RESIVE
 * subtotal (number) este parametro que resive el subtotal(la suma de 
 * las cantidades que obtiene el subtotal) cuantoEsDeIva (number) 
 * este parametro risive el iva que se le saco a el subtotal.
 * 
 * DEVUELVE
 * El total devuelve lo que es las suma de subtotal más iva = total.
 * 
 * FUNCION
 * Esta funcion es la que me permite saber cuanto necesito pagar en 
 * total más iva.  
 */